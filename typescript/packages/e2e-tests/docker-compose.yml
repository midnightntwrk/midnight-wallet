services:
  proof-server:
    image: ghcr.io/midnight-ntwrk/proof-server:2.0.7
    container_name: proof-server
    ports:
      - '6300:6300'
  node:
    image: ghcr.io/midnight-ntwrk/midnight-node:bbe1f8ec774b360033d24ede3cd0b8d501daeb09
    container_name: node
    environment:
      - RUN_DEV_NET=TRUE
    ports:
      - '9933:9933'
      - '9944:9944'
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9933/health" ]
      interval: 2s
      timeout: 5s
      retries: 5
      start_period: 40s
  indexer:
    image: ghcr.io/midnight-ntwrk/midnight-pubsub-indexer:0.2.20
    container_name: indexer
    ports:
      - '8088:8088'
    command: ["midnight-pubsub-indexer", "-Devents-source.node-host=node"]
    environment:
      - SUBSTRATE_NODE_URL=node
    depends_on:
      node:
        condition: service_healthy
