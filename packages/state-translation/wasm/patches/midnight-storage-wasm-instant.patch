--- a/Cargo.toml	2026-09-23 16:49:13
+++ b/Cargo.toml	2026-09-23 16:49:14
@@ -2,8 +2,8 @@
 name = "midnight-storage"
 edition = "2024"
 version = "2.0.4"
-license.workspace = true
-repository.workspace = true
+license = "Apache-2.0"
+repository = "https://github.com/midnightntwrk/midnight-ledger"
 description = "Provides data structures and storage primitives for Midnight's ledger."
 
 [features]
@@ -60,3 +60,6 @@
 name = "stress"
 path = "bin/stress.rs"
 required-features = ["stress-test"]
+
+[target.'cfg(all(target_arch = "wasm32", target_os = "unknown"))'.dependencies]
+web-time = "^1.1.0"
--- a/src/state_translation.rs	2026-09-23 16:49:13
+++ b/src/state_translation.rs	2026-09-23 16:49:14
@@ -26,7 +26,12 @@
 use std::io;
 use std::marker::PhantomData;
 use std::sync::atomic::AtomicU64;
+// `std::time::Instant` is unimplemented on wasm32-unknown-unknown and traps when called. `web-time` is a drop-in
+// replacement backed by `performance.now()` there, and a re-export of `std::time` everywhere else.
+#[cfg(not(all(target_arch = "wasm32", target_os = "unknown")))]
 use std::time::Instant;
+#[cfg(all(target_arch = "wasm32", target_os = "unknown"))]
+use web_time::Instant;
 
 pub type RawNode<D> = ArenaKey<<D as DB>::Hasher>;
 
