{
  "name": "cross-release-corpus",
  "private": true,
  "type": "module",
  "description": "Generates snapshot fixtures using the last SDK release on the ledger-v8 line. Deliberately outside the workspace.",
  "scripts": {
    "generate": "node generate.mjs"
  },
  "dependencies": {
    "@midnightntwrk/wallet-sdk": "1.2.0"
  }
}
