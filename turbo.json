{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_AUTH_TOKEN", "MIDNIGHT_GH_USER", "MIDNIGHT_GH_TOKEN"],
  "tasks": {
    "lint": {
      "inputs": [
        "src/**/*.{ts,tsx}",
        "test/**/*.{ts,tsx}",
        "scripts/**/*.ts",
        "tsconfig*.json",
        "$TURBO_ROOT$/tsconfig.base.json",
        "eslint.config.mjs",
        "$TURBO_ROOT$/eslint.config.mjs"
      ],
      "outputLogs": "new-only",
      "dependsOn": ["^dist", "typecheck"]
    },
    "test": {
      "inputs": [
        "test/**/*.{ts,tsx}",
        "src/**/*.{ts,tsx}",
        "tsconfig*.json",
        "$TURBO_ROOT$/tsconfig.base.json",
        "vitest.config.mjs"
      ],
      "outputs": ["coverage/**", "reports/**"],
      "env": ["DEBUG", "APP_INFRA_SECRET"],
      "outputLogs": "new-only",
      "dependsOn": ["^dist", "typecheck"]
    },
    "typecheck": {
      "inputs": [
        "test/**/*.{ts,tsx}",
        "src/**/*.{ts,tsx}",
        "scripts/**/*.ts",
        "tsconfig*.json",
        "$TURBO_ROOT$/tsconfig.base.json"
      ],
      "outputLogs": "new-only",
      "dependsOn": ["^dist"]
    },
    "test-undeployed": {
      "cache": false,
      "env": ["NETWORK", "APP_INFRA_SECRET"]
    },
    "test-remote": {
      "cache": false,
      "env": [
        "NETWORK",
        "SEED",
        "SEED2",
        "SEED_STABLE",
        "ADDRESSES",
        "NT_SEED",
        "NT_SEED2",
        "SYNC_CACHE",
        "APP_INFRA_SECRET"
      ]
    },
    "dist": {
      "inputs": [
        "index.html",
        "src/**/*.{ts,tsx}",
        "tsconfig.json",
        "tsconfig.build.json",
        "$TURBO_ROOT$/tsconfig.base.json",
        ".clean-timestamp",
        "vite.config.ts"
      ],
      "outputs": ["dist/**", "tsconfig.*.tsbuildinfo"],
      "outputLogs": "new-only",
      "dependsOn": ["^dist"]
    },
    "dist:publish": {
      "inputs": [
        "src/**/*.ts",
        "tsconfig.json",
        "tsconfig.build.json",
        "tsconfig.publish.json",
        "$TURBO_ROOT$/tsconfig.base.json",
        ".clean-timestamp"
      ],
      "outputs": ["dist/**", "tsconfig.*.tsbuildinfo"],
      "outputLogs": "new-only",
      "dependsOn": ["^dist:publish"]
    },
    "publint": {
      "inputs": [
        "src/**/*.ts",
        "tsconfig.json",
        "tsconfig.build.json",
        "tsconfig.publish.json",
        "$TURBO_ROOT$/tsconfig.base.json"
      ],
      "outputs": [],
      "outputLogs": "new-only",
      "dependsOn": ["dist:publish"]
    },
    "clean": {
      "cache": false,
      "inputs": [],
      "outputs": [],
      "outputLogs": "new-only"
    },
    "//#format:root": {
      "cache": false,
      "inputs": ["**/*.{ts,js,json,yaml,yml,md}", ".prettierrc.json", ".prettierignore.root"],
      "outputs": [],
      "outputLogs": "new-only"
    },
    "//#format:check:root": {
      "cache": false,
      "inputs": ["**/*.{ts,js,json,yaml,yml,md}", ".prettierrc.json", ".prettierignore.root"],
      "outputs": [],
      "outputLogs": "new-only"
    },
    "format": {
      "inputs": ["**/*.{ts,tsx,js,json,yaml,yml,md}", ".prettierrc.json", "**/.prettierignore"],
      "outputs": [],
      "outputLogs": "new-only",
      "dependsOn": ["//#format:root"]
    },
    "format:check": {
      "inputs": ["**/*.{ts,tsx,js,json,yaml,yml,md}", ".prettierrc.json", "**/.prettierignore"],
      "outputs": [],
      "outputLogs": "new-only",
      "dependsOn": ["//#format:check:root"]
    },
    "verify:check": {
      "dependsOn": ["typecheck", "format:check", "lint"]
    },
    "verify:test": {
      "dependsOn": ["test"]
    },
    "verify": {
      "dependsOn": ["verify:check", "verify:test"]
    }
  }
}
