{
  "name": "midnight-wallet",
  "private": true,
  "version": "0.0.1",
  "packageManager": "yarn@3.1.1",
  "workspaces": [
    "api",
    "impl",
    "example"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -pt run build",
    "test": "yarn workspaces foreach run test",
    "test_coverage": "yarn workspaces foreach run test_coverage",
    "coverage_report": "yarn workspaces foreach run coverage_report",
    "check_format": "yarn workspaces foreach run check_format"
  }
}
