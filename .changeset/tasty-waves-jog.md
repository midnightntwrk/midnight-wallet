---
---

Combined renovate dependency updates: bumps devDependencies (`@effect/cluster`, `@graphql-codegen/*` to v6/v7, `tar-stream`), root resolutions (`protobufjs` v8, `immutable` v5, `undici` v8, `@substrate/connect` v2), the `actions/setup-node` CI action to v6.4.0, and refreshes the yarn lock file. No published-package source changes.
